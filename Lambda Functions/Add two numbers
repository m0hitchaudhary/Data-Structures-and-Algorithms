#include <bits/stdc++.h> 
using namespace std; 
int main() 
{ 
    int t; cin>>t;
    
    while(t--){
        int x;int y;
        cin>>x>>y;

    auto add = [](int a, int b){
      return a+b;
    };
        
        cout<<add(x,y)<<endl;
    }
  
} 
